interface:
  display_name: "feymanlee/slog"
  short_description: "Production logging workflows for feymanlee/slog"
  default_prompt: "Use $feymanlee-slog to configure production JSON logging with DLP, trace context, and safe slog-compatible APIs."
