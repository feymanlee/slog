// Template: basic feymanlee/slog usage.
package main

import "github.com/feymanlee/slog"

func main() {
	slog.SetLevelDebug()

	slog.Info("service started", "port", 8080, "env", "dev")
	slog.Debug("cache warmed", "keys", 42)
	slog.Warn("dependency slow", "name", "payment")
	slog.Error("request failed", "error", "timeout")
}
